The task or resource filters are stored in an XML file, example:

<filters>
    <filter name="example filter" type="0|1" fop="0|1">
                                  <!-- see filter.h:enum FilterType -->
                                             <!-- filter operator, filter.h:enum FilterOp-->
	<conditions>
	    <condition property="taskName" expression="0" value="blah" />
                   <!-- task/resource property -->
                                       <!-- see filter.h:enum FilterExp -->
	    <condition property="responsible" expression="2" value="John Doe" />
	</conditions>
    </filter>
</filters>
